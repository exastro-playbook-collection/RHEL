---
#default vars
# VAR_RH_name_resolve_hosts: "{{ VAR_name_resolve_hosts | default([]) }}"
# VAR_RH_name_resolve_dns: "{{ VAR_name_resolve_dns | default([]) }}"
VAR_RH_name_resolve_ipv6_disabled: false
